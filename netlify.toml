[build]
  command = "npm run build"
  publish = "dist"

# SPA（クライアントサイドルーティング）のため、存在しないパスへのアクセスは
# すべてindex.htmlにフォールバックさせる（react-router-domが以降のルーティングを処理する）。
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
